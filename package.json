{
  "devDependencies": {
    "@biomejs/biome": "^1.7.3",
    "@types/google-apps-script": "^1.0.91",
    "clasp": "^1.0.0"
  },
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome lint .",
    "check": "biome check .",
    "deploy": "clasp push && clasp open"
  }
}
